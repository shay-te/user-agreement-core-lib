# @package _global_
core_lib:
  user_agreement_core_lib:
    data:
      agreement_db:
        log_queries: false
        create_db: true
        session:
          pool_recycle: 3200
          pool_pre_ping: false
        url:
          protocol: sqlite
  alembic:
    script_location: data_layers/data/agreement_db/migrations